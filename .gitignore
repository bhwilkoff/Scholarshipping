# Runtime state — these are generated during use, not part of the template
docs/summaries/
docs/archive/
output/

# .claude runtime state — these are generated, not part of the template
.claude/agents/
# settings.json ships with the template (it configures the session-start hook)
# exclude only user-local overrides
.claude/settings.local.json

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# The research file used to build the README (not part of the public repo)
Claude-Compaction-Problem.md
